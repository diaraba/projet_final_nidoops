  <%= form_with(model: [structure, abonnement] ) do |form| %>

    <%= form.hidden_field :structure_id, value: structure.id %>
    <%= form.hidden_field :user_id, value: current_user.id %>
    <div class="actions">
      <%= form.submit "S'abonner" , class:"btn btn-success"%>
    </div>
  <% end %>