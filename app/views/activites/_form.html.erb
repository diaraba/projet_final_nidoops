<%= form_with(model: activite, local: true) do |form| %>
  <% if activite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activite.errors.count, "error") %> prohibited this activite from being saved:</h2>

      <ul>
        <% activite.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-floating mt-4">
    <%= form.label :nom %>
    <%= form.text_field :name , placeholder: "Entrer le nom...", class: "form-control", id: "inputNom"%>
  </div>


  <div class="form-floating mt-4 submit">
    <%= form.submit "Créer", class: "btn btn-success"%>
  </div>
<% end %>
