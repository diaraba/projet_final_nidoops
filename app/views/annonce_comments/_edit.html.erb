<%= render 'layouts/flash' %>
<div class="comment_edit_form">
  <%= form_with(model: [annonce, comment]) do |form| %>
    <div class="field">
      <%= form.text_area :description, id: "comment_description_#{annonce.id}" %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>

    <%= link_to :back do %>
      Annuler
    <% end %>
  <% end %>
</div>